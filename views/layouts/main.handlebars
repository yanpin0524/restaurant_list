<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Restaurant_list</title>

  <link rel="shortcut icon"
    href="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png"
    type="image/x-icon">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.9/css/all.css"
    integrity="sha384-5SOiIsAziJl6AWe0HWRKTXlfcSHKmYV4RBF18PPJ173Kzn7jzMyFuTtk8JA7QQG1" crossorigin="anonymous">
  </link>
  <link rel="stylesheet" href="/stylesheets/bootstrap.css">
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <header>
    <!-- navigation -->
    <nav class="navbar navbar-light bg-light fixed-top">
      <div class="container-fluid">
        <a class="navbar-brand d-flex align-items-center" href="/">
          <img src="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png"
            width="30" height="30" class="d-inline-block align-top me-2" alt="我的餐廳清單">
          {{#if isAuthenticated}}
          {{#if user.name }}{{ user.name }}的餐廳清單
          {{else}}
          我的餐廳清單
          {{/if}}
          {{else}}
          餐廳清單
          {{/if}}
        </a>
        {{#if isAuthenticated}}
        <a href="/users/logout" class="btn rounded-pill text-light" style="background-color: #459BC6;">登出</a>
        {{else}}
        <a href="/users/login" class="btn rounded-pill text-light" style="background-color: #459BC6;">登入</a>
        {{/if}}
      </div>
    </nav>

    <div class="banner"></div>
  </header>

  {{{body}}}

  {{!-- javaScript --}}
  <script src="/javascripts/popper.js"></script>
  <script src="/javascripts/bootstrap.js"></script>
</body>

</html>